{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "yarn install --ignore-engines --frozen-lockfile",
        "buildCommand": "yarn build"
      }
    }
  ],
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
