rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is a temporary rule for testing and is not secure.
    // It allows all reads and writes to all collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
